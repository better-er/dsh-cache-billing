# dsh-cache-billing bundle patch: inserts this plugin into a profile's layer stack.
# Applied when a profile lists this bundle (or wired by dev_install_package).
- insert:
    - id: dsh-cache-billing
      name: 'dsh-cache-billing'
      config:
        enabled: true
